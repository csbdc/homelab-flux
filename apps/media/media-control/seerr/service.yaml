apiVersion: v1
kind: Service
metadata:
  name: seerr-prod-media-svc
  namespace: media-control
spec:
  selector:
    app: seerr
  ports:
    - port: 5055
      targetPort: http
      name: http
      protocol: TCP
