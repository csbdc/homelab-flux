apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: kustomization

resources:
  - ../../arr-base

namePrefix: sonarr-
namespace: media

patches:
  - path: service.yaml
  - path: deployment.yaml
  - path: secrets.yaml
