apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-prod-media-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Mi
