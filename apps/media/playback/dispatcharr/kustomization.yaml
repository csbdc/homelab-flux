apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: kustomization

namespace: media

resources:
  - ciliumNetworkPolicies.yaml
  - persistentVolumeClaim.yaml
  - service.yaml
  - configMap.yaml
  - postgres.yaml
  - helmRepository.yaml
  - helmRelease.yaml
  - deployment.yaml
  - ingress.yaml