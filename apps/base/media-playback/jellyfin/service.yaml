apiVersion: v1
kind: Service
metadata:
  name: jellyfin-prod-media-svc
  namespace: media
spec:
  selector:
    app: jellyfin
  ports:
    - port: 8096
      targetPort: http
      name: http
      protocol: TCP
