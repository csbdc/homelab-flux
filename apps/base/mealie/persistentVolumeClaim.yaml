apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-prod-pvc
  namespace: mealie
  labels:
    app: mealie
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi