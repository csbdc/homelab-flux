apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfs-server-provisioner
  namespace: default
spec:
  install:
    createNamespace: true
  targetNamespace: media-share
  interval: 10m
  chart:
    spec:
      chart: nfs-server-provisioner
      sourceRef:
        kind: HelmRepository
        name: nfs-ganesha-server-and-external-provisioner
        namespace: default
      interval: 10m
