apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: dispatcharr
    tier: frontend
  name: dispatcharr-prod-frontend-deployment
  namespace: dispatcharr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dispatcharr
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: dispatcharr
        tier: frontend
    spec:
      containers:
        - name: web
          image: ghcr.io/dispatcharr/dispatcharr:latest
          env:
            - name: POSTGRES_HOST
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: host
            - name: POSTGRES_PORT
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: port
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: dbname
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: password
          envFrom:
          - configMapRef:
              name: dispatcharr-prod-web-config
          ports:
            - containerPort: 9191
              name: http
              protocol: TCP
          resources: {}
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: dispatcharr-prod-pvc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: dispatcharr
    tier: app
  name: dispatcharr-prod-celery-deployment
  namespace: dispatcharr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dispatcharr
      tier: app
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: dispatcharr
        tier: app
    spec:
      containers:
        - name: celery
          image: ghcr.io/dispatcharr/dispatcharr:latest
          command: 
            - "/app/docker/entrypoint.celery.sh"
          env:
            - name: POSTGRES_HOST
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: host
            - name: POSTGRES_PORT
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: port
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: dbname
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dispatcharr-prod-pg-app
                  key: password
          envFrom:
            - configMapRef:
                name: dispatcharr-prod-celery-config
          resources: {}
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: dispatcharr-prod-pvc