apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-prod-media-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Mi
