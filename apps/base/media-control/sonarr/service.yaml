apiVersion: v1
kind: Service
metadata:
  name: sonarr-prod-media-svc
  namespace: media
spec:
  selector:
    app: sonarr
  ports:
    - port: 8989
      targetPort: http
      name: http
      protocol: TCP
