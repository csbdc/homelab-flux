apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-arpa-tls
spec:
  secretName: home-arpa-tls
  issuerRef:
    kind: ClusterIssuer
    name: home-ca-issuer
  commonName: home.arpa
  dnsNames:
    - frigate.home.arpa
